x-legion-db-env: &legion-db-env
  DATABASE_URL: "postgresql+psycopg://legion:legion@postgres:5432/legion"
  LEGION_DB_URL: "postgresql+psycopg://legion:legion@postgres:5432/legion"

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: legion
      POSTGRES_PASSWORD: legion
      POSTGRES_DB: legion
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "legion"]
      interval: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 5

  migrate:
    build: .
    command: legion-cli db upgrade
    environment:
      <<: *legion-db-env
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  api:
    build: .
    command: legion-api
    ports:
      - "8000:8000"
    environment:
      <<: *legion-db-env
      API_HOST: "0.0.0.0"
      API_PORT: "8000"
      API_LOG_LEVEL: "DEBUG"
      API_LOG_FORMAT: "TEXT"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/health/ready').raise_for_status()"]
      interval: 10s
      retries: 5
      start_period: 10s

  # Future: React frontend
  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"
  #   depends_on:
  #     api:
  #       condition: service_healthy

  agent-runner:
    build: .
    command: python local/compose_agent_runner.py
    environment:
      LEGION_API_URL: "http://api:8000"
      LEGION_AGENT_NAME: "demo-agent-1"
      AGENT_RUNNER_CAPABILITIES: "demo"
      AGENT_RUNNER_LOG_LEVEL: "INFO"
      AGENT_RUNNER_LOG_FORMAT: "TEXT"
    depends_on:
      api:
        condition: service_healthy
    profiles:
      - demo

  demo-agent:
    build: .
    command: python local/demo_agent.py demo-agent-1
    environment:
      LEGION_API_URL: "http://api:8000"
    depends_on:
      api:
        condition: service_healthy
    profiles:
      - demo-legacy

volumes:
  pgdata:
